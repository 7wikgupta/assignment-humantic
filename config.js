const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "db4free.net",
    user: "satwik",
    password: "Admin@123",
    database: "humantic",
  },
  listPerPage: 10,
};
module.exports = config;